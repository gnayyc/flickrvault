[project]
name = "flickrvault"
version = "2.0.0"
description = "Full Flickr backup tool with metadata, albums, and visual browsing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flickr-api>=0.8.0",
]
